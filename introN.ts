/* ================================================================ */
/* introN.ts is part of the TypeScript Hello World tutorial. */
/* ================================================================ */

// Examples
//let ex_Int_enums : string = "Int enum";

// Set example
let exampleN : string = ex_Int_enums;
switch( exampleN ){

   /* TOKEN
      ================================================================ *
      case "TOKEN":
         console.log( "<<<TOKEN>>>" );
      
      
         console.log( "" );
         break;

   */

   default:
      console.log( "No example selected. Assign an example." );
}